apiVersion: v1
kind: Service
metadata:
  name: healthra-frontend
  namespace: healthra
  labels:
    app: healthra-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: healthra-frontend

